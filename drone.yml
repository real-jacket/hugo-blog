type: docker
kind: pipeline
name: default

steps:
- name: submodule
  image: plugins/git
  settings:
    depth: 0
    recursive: true
- name: hugo-build
  image: plugins/hugo
  settings:
    hugo_version: latest
    validate: true
    theme: FixIt
  commands:
   - hugo -v --gc --minify