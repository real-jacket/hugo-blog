type: docker
kind: pipeline
name: default

steps:
- name: submodule
  image: plugins/git
  settings:
    depth: 0
    recursive: true

- name: hugo-build
  image: plugins/hugo
  settings:
    hugo_version: 0.96.0+extended
    validate: true
    theme: FixIt
  commands:
  - hugo -v --gc --minify